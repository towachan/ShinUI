
{{#if isLogin}}
  {{outlet}} 
{{/if}}

{{#unless isLogin}}
{{#nav-footer isLeave=isLeave isTravel=isTravel user=currentUserName switch=(action "switchSystem") logout=(action "jumpToLogin")}}
{{outlet}}
{{/nav-footer}}
{{/unless}}

{{alert-modal header=modalHeader msg=modalMsg isReload=modalReload}}